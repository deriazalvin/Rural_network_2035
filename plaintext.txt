Village
- Modifier 
- Interaction carte (regarder sur carte , afficher popup avec stats)
recherche par nom
filtre par production (haut / bas)
tri :
plus productifs
moins productifs
récents
confirmation suppression village
détection doublons de villages

“ce village est top 3 production”
“faible production détectée”
comparaison instantanée entre villages

Frontend (GestionRoutes) 
  ↓ POST /api/routes
Backend (RouteService.ajouterRoute)
  ↓ appel
OsrmRoutingService.obtenirDistanceRoutiere()
  ↓ HTTP GET
API OSRM (localhost:5000)
  ↓ réponse JSON
Conversion: mètres → km
  ↓ save
Base de données (distance calculée réelle)

docker run -t -i -p 5000:5000 -v $(pwd):/data osrm/osrm-backend osrm-routed --algorithm mld /data/madagascar-latest.osrm
 

